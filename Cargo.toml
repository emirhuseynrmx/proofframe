[package]
name = "proofframe"
version = "0.7.1"
edition = "2024"
rust-version = "1.85"
description = "Rust-native Arrow contracts, exact checks, fingerprints, and verifiable evidence"
license = "Apache-2.0"
repository = "https://github.com/emirhuseynrmx/proofframe"
homepage = "https://github.com/emirhuseynrmx/proofframe"
documentation = "https://docs.rs/proofframe"
readme = "README-crates.md"
keywords = ["arrow", "data-quality", "validation", "parquet", "evidence"]
categories = ["data-structures", "development-tools", "science"]
include = [
  "/src/**",
  "/tests/*.rs",
  "/tests/support/**",
  "/docs/testing.md",
  "/examples/**",
  "/Cargo.toml",
  "/Cargo.lock",
  "/README.md",
  "/README-crates.md",
  "/CHANGELOG.md",
  "/LICENSE",
  "/SECURITY.md",
]

[lib]
name = "proofframe"
crate-type = ["cdylib", "rlib"]

[features]
default = []
python = ["dep:pyo3", "arrow/pyarrow"]
fuzzing = []

[dependencies]
ahash = "0.8.12"
arrow = { version = "=59.1.0", default-features = false, features = ["ipc"] }
base64 = "0.22.1"
blake3 = "1.8.2"
chrono = { version = "0.4.42", default-features = false, features = ["std"] }
ed25519-dalek = "3.0.0"
getrandom = "0.4.3"
pyo3 = { version = "0.29.0", optional = true, features = ["abi3-py310"] }
regex = "1.12.2"
serde = { version = "1.0.228", features = ["derive"] }
serde_json = { version = "1.0.149", features = ["arbitrary_precision"] }
serde_json_canonicalizer = "0.3.2"
tempfile = "3.27.0"
thiserror = "2.0.17"

[dev-dependencies]
proptest = "1.9.0"

[profile.release]
lto = "thin"
codegen-units = 1
strip = true
